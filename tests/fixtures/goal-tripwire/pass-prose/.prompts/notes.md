This brief references the /goal feature in passing — mid-line, exempt.
> /goal discussed inside a blockquote is prose, not an executable line.
`/goal backticked at line start` is a code-span mention, also exempt.
