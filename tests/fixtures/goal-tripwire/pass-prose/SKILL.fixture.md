# Fixture: prose /goal mentions must NOT trip the lint (must PASS)

The `/goal` command is mentioned here mid-sentence — not an executable line.

══════════════════ START 🟢 COPY ══════════════════
Run the brief as written. The `/goal` line stays in chat only, never in this fence.
This block explains /goal usage mid-line — still exempt from the line-start anchor.
══════════════════= END 🛑 COPY ═══════════════════
