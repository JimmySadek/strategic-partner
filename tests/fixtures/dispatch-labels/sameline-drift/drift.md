# Fixture — an incorrect label sharing a line with a correct one

The statement below presents both spellings as usable options. Nothing on the
line or above it marks either one as text to steer clear of, so the bare form
is a real instruction rather than a contrast.

Gate the dispatch with `[Dispatch now - backend-architect]` or `[Dispatch now]`,
whichever reads better in the option list.

The other two conformant labels, so the scan covers all three keywords:
`[Hold - let me review the brief first]` and `[Wrong agent - let me pick]`.
