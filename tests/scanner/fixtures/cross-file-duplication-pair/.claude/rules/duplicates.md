# Duplicates File

A non-canonical rules file that re-defines the same rules — should
trigger B7 cross-file dedup (NOT canonical hybrid because path is
non-source-editing).

## Think Before Coding

State assumptions. Surface confusion. Don't pick silently when
multiple interpretations exist. Push back when warranted.

## Surgical Changes

Touch only what was asked. Match existing style. No drive-by edits.
