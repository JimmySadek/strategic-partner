# Removed-Feature References

The rules below reference flags / functions / env vars that don't
exist in the project.

## Where to Look

- The `--legacy-mode` flag triggers the old code path.
- Use `${OLD_API_TOKEN}` for upstream calls.
- `deprecated_function()` returns the cached value.
