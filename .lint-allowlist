# Lint allowlist for transcript voice rules
# Each non-comment, non-blank line is a JSONL filename (basename, no path)
# that the transcript lint should skip. Use sparingly — entries should be
# deliberate decisions, documented in CHANGELOG.

# v5.17.0 bootstrap cohort:
# Two old session transcripts accumulated voice-rule violations during
# advisory work in the v5.17.0 design phase before voice discipline was
# fully internalized in advisory prose patterns. The shippable release
# artifacts (CHANGELOG, README, CLAUDE.md, claudedocs/INCIDENTS.md) are
# voice-clean; only the chat transcripts contained drift. Future sessions
# are expected to stay clean — see backlog item
# in-chat-voice-runtime-enforcement for the structural fix.
90b4e97e-fc41-489e-b1ac-d2d18f2c0a47.jsonl
fd34f851-d39c-4f2c-84e7-a93f1f797168.jsonl
